---
sidebar_position: 2
title: Data Normalization
sidebar_label: N to 1
description: Transform log from multiple sources into a standardized format
image: img/api-autoflow-logo.png
keywords:
  - data normalization
  - data transformation
---

import n_to_1_example from '@site/static/img/example/data-transformation/n-to-1-example.png';
import n_to_1_explanation from '@site/static/img/example/data-transformation/n-to-1-explanation.png';
import n_to_1_return from '@site/static/img/example/data-transformation/n-to-1-return.jpg';
import n_to_1_case from '@site/static/img/example/data-transformation/n-to-1-case.jpg';

# N to 1

Received data from multiple sources in a different format and normalizes the data into a standardized data format.


## Data Objective

Duplicate data is a common problem that data normalization needs to solve.  In this example, a user data in to different format is normalized into a standardized JSON format.

<img src={n_to_1_example} alt="N to 1 example" class="myResponsiveImg" width="800px"/>

&nbsp;<br />

:::info Process
1. The process will be to use [conditional/switch](Documentation/actions-library/flow/conditional/action-conditional-switch) action to detect the data format
2. Than apply various actions to transform the value(s).
:::

## Transforming the data

1. First detect the value using [conditional/switch](Documentation/actions-library/flow/conditional/action-conditional-switch).<br />
Set the <span class="action-field">SWITCH</span> condition to the input argument `source`
2. Second, apply <span class="action-field">CASE</span> where 'source' = 'website_user'
3. Third, for all other 'source', apply a <span class="action-field">DEFAULT</span> case.

<img src={n_to_1_explanation} alt="N to 1 explanation" class="myResponsiveImg" width="800px"/>

## CASE: String split

In the first case, the name is joined in a single string.  Use [string/split](Documentation/actions-library/data/string/action-string-split.mdx) to split on every white space.

<img src={n_to_1_case} alt="N to 1 case" class="myResponsiveImg" width="800px"/>

## DEFAULT: Map the fields

Create a return object and map the values in the new JSON object.

<img src={n_to_1_return} alt="N to 1 return" class="myResponsiveImg" width="800px"/>

&nbsp;<br />

:::note Download This Example

[Download](@site/static/file/config-data-transformation-example.json) configuration for this example and [upload](../../../Documentation/Guide/Settings/#upload-configuration) to your API AutoFlow

Learn how to [upload](../../../Documentation/Guide/Settings/#upload-configuration) configuration.
:::